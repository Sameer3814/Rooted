# Copy this file to pipeline/.env.local (gitignored, never committed) and
# paste your real key in place of the placeholder below.
#
# Get a key at https://aistudio.google.com/apikey — image generation
# specifically needs billing enabled on the project (see "Set up billing"
# on that page; the free tier does not cover image output).

GEMINI_API_KEY=paste-your-key-here
