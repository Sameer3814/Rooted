.DS_Store
Thumbs.db
*.log
node_modules/
__pycache__/
pipeline/.cache/

# Azure Functions local dev — can hold real secrets for local testing;
# never commit. api/local.settings.json.example is the checked-in template.
api/local.settings.json
api/bin/
api/obj/

# Gemini API key for pipeline/generate_character_art.py — never commit.
# pipeline/.env.local.example is the checked-in template.
pipeline/.env.local
pipeline/.artscratch/

# Raw downloads from the Colab SDXL story-art notebook, before
# pipeline/import_story_art.py resizes/moves them into media/stories/.
pipeline/.storyart_incoming/
